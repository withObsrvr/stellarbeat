name: stellarbeat-integration
services:
  - name: frontend
    git:
      branch: integration
      repo_clone_url: ${GITHUB_REPO_URL}
    source_dir: apps/frontend
    dockerfile_path: Dockerfile
    http_port: 3000
    instance_count: 1
    instance_size_slug: basic-xs
    routes:
      - path: /
    envs:
      - key: NODE_ENV
        value: integration
      - key: VITE_API_URL
        value: https://api-integration.${DOMAIN}
      - key: VITE_WS_URL
        value: wss://api-integration.${DOMAIN}

  - name: backend
    git:
      branch: integration
      repo_clone_url: ${GITHUB_REPO_URL}
    source_dir: apps/backend
    dockerfile_path: Dockerfile
    http_port: 3000
    instance_count: 1
    instance_size_slug: basic-xs
    routes:
      - path: /api
    envs:
      - key: NODE_ENV
        value: integration
      - key: PORT
        value: "3000"
      - key: DATABASE_URL
        scope: RUN_TIME
        type: SECRET
      - key: JWT_SECRET
        scope: RUN_TIME
        type: SECRET

  - name: network-scanner
    git:
      branch: integration
      repo_clone_url: ${GITHUB_REPO_URL}
    source_dir: apps/backend
    dockerfile_path: Dockerfile.network-scanner
    instance_count: 1
    instance_size_slug: basic-xs
    envs:
      - key: NODE_ENV
        value: integration
      - key: DATABASE_URL
        scope: RUN_TIME
        type: SECRET

  - name: history-scanner
    git:
      branch: integration
      repo_clone_url: ${GITHUB_REPO_URL}
    source_dir: apps/history-scanner
    dockerfile_path: Dockerfile
    instance_count: 1
    instance_size_slug: basic-xs
    envs:
      - key: NODE_ENV
        value: integration
      - key: DATABASE_URL
        scope: RUN_TIME
        type: SECRET

  - name: users
    git:
      branch: integration
      repo_clone_url: ${GITHUB_REPO_URL}
    source_dir: apps/users
    dockerfile_path: Dockerfile
    http_port: 3000
    instance_count: 1
    instance_size_slug: basic-xs
    routes:
      - path: /users
    envs:
      - key: NODE_ENV
        value: integration
      - key: PORT
        value: "3000"
      - key: DATABASE_URL
        scope: RUN_TIME
        type: SECRET
      - key: JWT_SECRET
        scope: RUN_TIME
        type: SECRET

databases:
  - engine: PG
    name: stellarbeat-integration-db
    production: false
    version: "15" 